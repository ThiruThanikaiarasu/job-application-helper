# Local HTTP port. The default is 4319.
PORT=4319

# Google OAuth, Sheet, Gmail, Hunter, and Anthropic settings are configured
# in the Settings screen and stored locally in data/settings.json.
# Do not put real credentials in this file. ANTHROPIC_API_KEY is not read
# automatically by this version; enter it in Settings when using the API engine.
