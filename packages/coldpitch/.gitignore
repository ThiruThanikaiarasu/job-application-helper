# Personal config & data — never commit
config.md
job-applications.md

# Secrets (these live in ~/.config/coldpitch/, but guard against copies)
credentials.json
token.json
*.key

# Python / uv
__pycache__/
*.pyc
.venv/
.python-version

# OS
.DS_Store
